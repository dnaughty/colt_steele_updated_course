const names = ["  Timothee", "Darth_hater   ", "elton John    "] 

// const cleanNames = names.map(function(name){
//     return name.trim()
// })

function cleanNames(array) {
    array.map{
        return n.trim()
    }
}

const ratings = movies.map(function(rate){
    return rate.score
})
